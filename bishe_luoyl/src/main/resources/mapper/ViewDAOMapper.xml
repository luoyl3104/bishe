<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzu.dao.ViewDAO">

    <resultMap id="viewMap" type="View">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="picture" property="picture"/>
        <result column="score" property="score"/>
        <result column="path" property="path"/>
        <result column="des" property="des"/>
        <result column="type" property="type"/>
        <result column="upload_date" property="uploadDate"/>
        <association property="user" javaType="User">
            <id column="uid" property="id"/>
            <result column="username" property="username"/>
        </association>
        <association property="province" javaType="Province">
            <id column="pid" property="id"/>
            <result column="pname" property="name"/>
        </association>
    </resultMap>
    
    <!-- 查询 -->
    <select id="selectByPage" resultMap="viewMap">
        select v.id,v.name,v.picture,v.score,v.path,v.des,v.type,v.upload_date,u.id as uid,u.username,p.id as pid,p.name as pname
            from (select * from view limit #{start},#{rows}) v
            left join user u on v.user_id=u.id left join province p on v.province_id=p.id
    </select>
    
    <!--回显-->
    <select id="findOne" resultMap="viewMap" parameterType="String">
        select v.id,v.name,v.picture,v.score,v.path,v.des,v.type,v.upload_date,u.id as uid,u.username,p.id as pid,p.name as pname
            from view v left join user u on v.userid=u.id left join province p on v.province_id=p.id
            where v.id=#{id}
    </select>

</mapper>
